/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'images.macrumors.com', 'lh3.googleusercontent.com'],
    },
};

export default nextConfig;

